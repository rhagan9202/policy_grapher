# IDE / editor project files
.idea/
.vscode/
*.iml
*.swp

# Agent scratch (subagent-driven-development ledger, briefs, review packages)
.superpowers/
.claude/worktrees/

# Personal Claude Code overrides. `.claude/settings.json` IS committed — it carries the
# pre-commit docs-sync hook the whole repo shares. This file is per-machine only.
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/
.coverage
htmlcov/

# Node
node_modules/
.vite/
frontend/dist/
*.log
*.tsbuildinfo

# Local environment overrides
#
# `.env` used to be committed with a development default so a clean clone ran with
# `docker compose up` and no manual step — see ADR-010, which reverses that. Secrets are
# now generated locally by `scripts/init-env.sh`; a clean clone runs `./scripts/init-env.sh`
# once instead.
.env.local
.env.*.local

# Generated by scripts/init-env.sh — contains real secrets.
.env
